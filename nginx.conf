server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location /safemode/ {
        alias /usr/share/nginx/html/;
        try_files $uri $uri/ /safemode/index.html;
    }

    location /safemode {
        return 301 /safemode/;
    }
}